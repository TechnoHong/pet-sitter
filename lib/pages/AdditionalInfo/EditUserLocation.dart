import 'package:flutter/material.dart';

class EditUserLocation extends StatelessWidget {
  const EditUserLocation({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Page2");
  }
}