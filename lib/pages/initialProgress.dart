import 'package:flutter/material.dart';

class InitialProgress extends StatelessWidget {
  const InitialProgress ({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Loading...'),
    );
  }
}